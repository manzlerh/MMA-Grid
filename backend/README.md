## Backend

Planned **Node.js + Express** service that will:

- Expose REST endpoints for daily UFC grid and connections puzzles.
- Validate fighter selections against the database.
- Track anonymous user stats (scores, streaks, completion flags).
- Serve puzzle metadata and lightweight configuration to the frontend.

Implementation will follow the high-level architecture and data model described in `PLANNING.md` and the `Design/` documents.

